package controllers;

import model.Alternative;
import model.Choice;
import model.Feedback;

public class ChoiceController {
	boolean success;
	public ChoiceController(Choice c) {
		//TODO: add choice to model (doesnt exist) and determine success accordingly
		//possibly move parsing from controllerChain to here instead if easier
		
        success=false; 
	}
}
